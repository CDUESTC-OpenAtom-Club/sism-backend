-- adhoc_task_target clean seed
-- Scope:
-- - Junction table linking ad-hoc tasks to target organizations.
-- - Populated at runtime when tasks are created with target org assignments.

BEGIN;

-- Intentionally no seed rows.

COMMIT;
