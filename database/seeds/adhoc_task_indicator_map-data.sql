-- adhoc_task_indicator_map clean seed
-- Scope:
-- - Junction table linking ad-hoc tasks to indicators.
-- - Populated at runtime when tasks are created with indicator bindings.

BEGIN;

-- Intentionally no seed rows.

COMMIT;
